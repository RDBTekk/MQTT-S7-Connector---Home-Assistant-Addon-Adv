---
# Template file für die Konfigurations-GUI
name: MQTT Siemens S7 Connector
version: 1.0.9
slug: mqtt-s7-connector
description: "MQTT Siemens S7 connector to integrate Siemens PLC's with Home Assistant"
url: https://github.com/dixi83/hassio-addons/mqtt-s7-connector
init: false
services:
  - mqtt:need
discovery:
  - mqtt
arch:
  - aarch64
  - amd64
map:
  - addon_config:rw
options:
  log_level: warning
  test_mode: false
  config_files:
    - config.yaml
schema:
  log_level: list(trace|debug|info|notice|warning|error|fatal)
  test_mode: bool
  config_files:
    - str?
