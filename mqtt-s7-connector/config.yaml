---
# yamllint disable rule:line-length
name: MQTT Siemens S7 Connector
version: 1.0.11
slug: mqtt-s7-connector
description: "MQTT Siemens S7 connector to integrate Siemens PLC's with Home Assistant"
url: https://github.com/dixi83/hassio-addons/mqtt-s7-connector
init: false
services:
  - mqtt:optional
arch:
  - aarch64
  - amd64
map:
  - addon_config:rw
  - config:rw
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:cog
panel_title: MQTT S7 Konfiguration
options:
  log_level: warning
  test_mode: false
  config_files:
    - config.yaml
schema:
  log_level: list(trace|debug|info|notice|warning|error|fatal)
  test_mode: bool
  config_files:
    - str?
